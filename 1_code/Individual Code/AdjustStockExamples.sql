-- To use the procedure, type in the product ID, location, and how much to add.
-- Examples given below.

CALL AdjustStock(productID, 'sales_floor', stockAmount); 
CALL AdjustStock(productID, 'reserve', stockAmount);